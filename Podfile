# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'CropImagePicker' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!
  pod 'RSKImageCropper'

  # Pods for CropImagePicker

  target 'CropImagePickerTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'CropImagePickerUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
